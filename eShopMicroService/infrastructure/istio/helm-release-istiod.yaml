apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istiod
  namespace: flux-system
spec:
  chart:
    spec:
      chart: istiod
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: flux-system
        version: "1.22.0"
        interval: 5m
  dependsOn:
  - name: istio-base
  values:
    pilot:
    traceSampling: 1.0