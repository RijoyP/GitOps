apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: catelog-api
spec:
  host: catelog-api
  subsets:
    - name: blue
      labels:
        version: blue
    - name: green
      labels:
        version: green
